Notes: the scripts easyPassword, asciiConvert, and harderPassword do not require any libraries, but the stringHasher, hardestPassword, and hashCracker scripts require teh hashlib library to be installed. 
run the command pip install hashlib
 
